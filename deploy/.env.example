# Self-hosted Ciele configuration.
#
# You do not have to fill this in by hand: `./deploy/bootstrap.sh` writes a
# complete `deploy/.env` with every secret generated. This file documents what
# ends up there and what you may want to change afterwards.

# ── Which services run ───────────────────────────────────────────────────────
# The default stack. Add `studio` for the database admin UI, which is off
# unless listed here. The heavy graph + crawler workers are not a profile:
# they are an overlay file, see COMPOSE_FILE below.
COMPOSE_PROFILES=db,migrate,app,cron

# ── Which compose files, i.e. which overlays are on ──────────────────────────
# Unset (the default), the stack is docker-compose.yml alone: `app`, `migrate`
# and `cron` are built from the repo you checked out, and the heavy workers do
# not exist. There are two overlays, either or both:
#
#   docker-compose.images.yml   pull the three at a pinned release instead of
#                               building them, minutes instead of an afternoon
#   docker-compose.workers.yml  add the graph worker and the crawler
#
#   COMPOSE_FILE=docker-compose.yml:docker-compose.images.yml
#   CIELE_IMAGE_TAG=v0.4.0
#
# `./deploy/bootstrap.sh --images vX.Y.Z` and `--workers` write these lines for
# you, and neither switch turns the other one off.
#
COMPOSE_FILE=
CIELE_IMAGE_TAG=
# Override only to run a mirror or your own rebuild of the three images.
CIELE_IMAGE_REGISTRY=

# ── Where it is reachable ────────────────────────────────────────────────────
# The app's own public origin (used for auth redirects and email links). With
# a reverse proxy in front, set this to the public HTTPS URL.
PUBLIC_URL=http://localhost:3000
# The Supabase gateway origin the BROWSER uses. Must be reachable from the
# user's machine, not just from inside Docker.
SUPABASE_PUBLIC_URL=http://localhost:8000
APP_PORT=3000
SUPABASE_PORT=8000
STUDIO_PORT=8080
# Which host interface the published ports bind to. Loopback by default, so a
# stack on a VPS is not on the internet the moment it boots: put a TLS
# reverse proxy in front and let that be what the world reaches. Set to
# 0.0.0.0 only when you have decided plain HTTP on every interface is what you
# want. Studio ignores this and is always loopback-only.
BIND_ADDRESS=127.0.0.1
# The origin this install answers on, for links that go into an email. Without
# it the newsletter confirmation is not sent at all, rather than sent pointing
# at whatever the Host header claimed. Set it to the same public URL as above
# once a reverse proxy is in front.
CIELE_PUBLIC_ORIGIN=
# The app refuses to start when PUBLIC_URL, CIELE_PUBLIC_ORIGIN or
# NEXT_PUBLIC_APP_URL (the one a Vercel-style deploy sets) is http:// on a
# non-loopback host: cookies and signed links would travel in the clear.
# Set to 1 only for an install that has decided to run over plain HTTP (a
# LAN-only box, TLS terminated somewhere the app never sees).
CIELE_ALLOW_INSECURE_HTTP=

# ── TLS (only read when COMPOSE_FILE includes the tls overlay) ───────────────
# The shipped HTTPS path (#801, CYB-16): `./deploy/bootstrap.sh --tls` (or
# COMPOSE_FILE=docker-compose.yml:docker-compose.tls.yml) starts Caddy on
# 80/443, terminating TLS for these two names and proxying to the app and the
# Supabase gateway. DNS for both must point at this host. With TLS on, also
# set PUBLIC_URL, SUPABASE_PUBLIC_URL and CIELE_PUBLIC_ORIGIN to the matching
# https:// origins above.
CIELE_DOMAIN=
CIELE_SUPABASE_DOMAIN=

# ── Generated secrets (bootstrap.sh fills these) ─────────────────────────────
# Postgres superuser password, also used by the Supabase service roles.
POSTGRES_PASSWORD=
# Signs every Supabase JWT. ANON_KEY and SERVICE_ROLE_KEY below are JWTs
# signed with it, regenerate all three together or auth breaks.
JWT_SECRET=
ANON_KEY=
SERVICE_ROLE_KEY=
# Seals BYOK provider API keys at rest (AES-256-GCM).
APP_ENCRYPTION_KEY=
APPLICATION_IMPORT_MAX_REMOTE_ITEMS=100000
APPLICATION_IMPORT_MAX_ORG_BYTES=5368709120
APPLICATION_IMPORT_MAX_CONCURRENT=3
# Optional shared OAuth clients for Slack and personal drives. Salesforce and
# ServiceNow registrations are entered per Organization in the Connect form.
# See docs.ciele.app/knowledge/applications.
SLACK_APPLICATION_CLIENT_ID=
SLACK_APPLICATION_CLIENT_SECRET=
MICROSOFT_APPLICATION_CLIENT_ID=
MICROSOFT_APPLICATION_CLIENT_SECRET=
GOOGLE_APPLICATION_CLIENT_ID=
GOOGLE_APPLICATION_CLIENT_SECRET=
# Bearer token the cron container sends to the scheduled routes.
CRON_SECRET=

# ── Accounts ─────────────────────────────────────────────────────────────────
# The first account to sign up becomes the owner of its organization. Set this
# to also make that account the platform owner (cross-org settings).
PLATFORM_OWNER_EMAIL=
# Close signups once your team has accounts.
DISABLE_SIGNUP=false
# With no SMTP configured, signups are confirmed automatically so you can get
# in. Set SMTP_* below and flip this to false to require email confirmation.
MAILER_AUTOCONFIRM=true
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_ADMIN_EMAIL=

# ── AI providers ─────────────────────────────────────────────────────────────
# Any OpenAI-compatible server, Ollama, vLLM, LM Studio, a gateway, powers
# both chat and embeddings with no account anywhere. From inside Docker,
# reach a model server on the host as http://host.docker.internal:11434/v1.
OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_CHAT_MODEL=
# Without an embedding model, knowledge search falls back to lexical matching.
OPENAI_COMPATIBLE_EMBEDDING_MODEL=
# Or use a hosted provider directly.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=

# ── the workers (only read when COMPOSE_FILE includes the workers overlay) ───
# Graph worker: derived-graph retrieval. Without it, knowledge search uses
# vector + lexical retrieval only.
GRAPH_WORKER_BASE_URL=http://graph-worker:8000
GRAPH_WORKER_API_TOKEN=
GRAPH_LLM_PROVIDER=gemini
GRAPH_LLM_MODEL=gemini/gemini-2.0-flash
GRAPH_LLM_API_KEY=
# Crawl4AI: JavaScript-rendering website crawler. Without it, crawling uses
# the built-in fetch-based crawler.
CRAWL4AI_BASE_URL=http://crawl4ai:11235
CRAWL4AI_API_TOKEN=
CRAWL4AI_SECRET_KEY=
